% We begin our Great Adventure !

tables.

tables(Tables).

create(Table, Cols).

cols(Table, Cols).

rows(Table).

insert(Table, Row).

drop(Table).

delete(Table).

selec(TableOrTables, Selectors, Conds, Projection).
